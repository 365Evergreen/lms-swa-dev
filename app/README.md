# LMS SWA App

This is the Azure Static Web App deployment directory for the LMS project.

- Place all app source code in `/app/src`
- Place all static assets in `/app/public`
- Place workflow files in `/app/.github/workflows`
